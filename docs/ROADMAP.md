# Lộ trình 4 tuần

- Tuần 1: FE-001 danh sách chuyến → FE-002 Redux-Saga fetch + loading/error → FE-003 filter/sort + Reselect.
- Tuần 2: QA-001 test tương tác/API với Vitest và Testing Library → FE-004 form đặt chỗ → review/refactor.
- Tuần 3: BE-001 Express/PostgreSQL → BE-002 validation/phân quyền → BE-003 transaction và tránh đặt trùng ghế.
- Tuần 4: mock interview, dự án CV, hoàn thiện; Redis cache và Elasticsearch là mở rộng khi còn thời gian.

Chỉ FE-001 được đặc tả lúc này. Mỗi ticket tiếp theo được điều chỉnh sau review. Không cần học toàn bộ stack cùng lúc. React Native chưa thuộc MVP.

Quy trình: đọc ticket → đề xuất cách làm → tự code → review → sửa → giải thích 2–3 câu phỏng vấn.
